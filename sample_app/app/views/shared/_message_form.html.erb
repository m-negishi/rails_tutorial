<%= form_for(Message.new) do |q| %>
  <%= render 'shared/error_messages', object: q.object %>
  <div class="field">
    <%= q.text_area :content, placeholder: "Compose new message..." %>
  </div>
  <%= q.submit "Send", class: "btn btn-large btn-primary" %>
<% end %>
